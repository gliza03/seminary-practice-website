<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Memory Game</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }
        h1 {
            font-size: 36px;
            color: #333;
            margin-top: 40px;
            margin-bottom: 20px;
            text-align: center;
        }
        .game-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            width: 80%;
            max-width: 800px;
            background-color: white;
            border: 2px solid #003366;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }
        .card {
            width: 100px;
            height: 150px;
            margin: 10px;
            background-color: #003366;
            border: 2px solid #ccc;
            border-radius: 5px;
            cursor: pointer;
        }
        .card.flipped {
            background-color: white;
            color: #003366;
        }
        .game-message {
            font-size: 18px;
            color: #003366;
            margin-top: 20px;
            text-align: center;
        }
        .game-form {
            width: 80%;
            max-width: 800px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .dropdown {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }
        .submit-button {
            font-size: 18px;
            padding: 10px 20px;
            color: white;
            background-color: #003366;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .submit-button:hover {
            background-color: #002244;
        }
        .back-link {
            margin-top: 20px;
            font-size: 16px;
            color: #003366;
            text-decoration: none;
        }
        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Memory Game</h1>
    <form class="game-form" action="/submit-score" method="POST">
        <select class="dropdown" name="username" required>
            <option value="" disabled selected>Select your name</option>
            <% for (let iCount = 0; iCount < students.length; iCount++) {%>
                <option value="<%= students[iCount].student_code_name %>"><%= students[iCount].student_code_name %></option>
            <% }; %>
        </select>
        <div class="game-container">
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
            <div class="card"></div>
        </div>
        <div class="game-message">Match the cards to win!</div><br>
        <button type="submit" class="submit-button">Submit Score</button><br>
    </form>
    <a href="/" class="back-link">Back to Doctrinal Mastery Review</a>
</body>
</html>
