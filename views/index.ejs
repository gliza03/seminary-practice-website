<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctrinal Mastery Review</title>
    <style>
        /* Basic styling for the body and layout */
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        /* Title styling, placed near the top */
        h1 {
            font-size: 36px;
            color: #333;
            margin-top: 40px;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Quote box styling */
        .quote-box {
            width: 80%;
            max-width: 600px;
            background-color: white;
            border: 2px solid #003366; /* Navy border */
            border-radius: 8px;
            padding: 20px;
            color: #003366;
            font-size: 18px;
            text-align: center;
            margin-bottom: 60px; /* Increased margin for more spacing */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
        }

        /* Central button styling for Play Memory Game */
        .play-button {
            font-size: 24px;
            padding: 20px 50px;
            color: white;
            background-color: #003366; /* Navy blue */
            border: none;
            border-radius: 8px;
            cursor: pointer;
            margin-top: auto; /* Moves button further down */
            margin-bottom: 60px; /* Extra space at bottom for centering */
        }
        .play-button:hover {
            background-color: #002244; /* Slightly darker navy */
        }

        /* Login button styling, positioned in top-right corner */
        .login-button {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 16px;
            color: #003366;
            background-color: white;
            padding: 10px 20px;
            border: 2px solid #003366; /* Navy border */
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }
        .login-button:hover {
            background-color: #003366;
            color: white;
        }
    </style>
</head>
<body>

    <!-- Title at the top of the page -->
    <h1>Doctrinal Mastery Review</h1>

    <!-- Quote box -->
    <div class="quote-box">
        "Doctrinal Mastery is an inspired and timely initiative that will have a wonderful influence on our young people" - President Ballard
    </div>

    <!-- Play Memory Game Button in the center, with more space to be visually centered -->
    <a class="play-button" href="memoryGame.ejs">Play Memory Game</button>

    <!-- Login Button in the top-right corner, linked to login.html -->
    <a href="login.ejs" class="login-button">Login</a>

</body>
</html>
